import java.util.*;
// class Thirty{}

class DemoArrayList{
    public static void main(String[] args) {
        ArrayList <Double> all = new ArrayList<>();
        all.add(10.0);
        all.add(20.0);
        all.add(30.0);
        all.add(40.0);
        all.add(2,20.0);
        // all.add(new Thirty());

        // int size()
        // void clear()
        // void ensureCapacity(int x)

        // Object.add()
        // Object.add(int index, <> element)
        // Object.remove(int index)
            // passing the value of object deletes the object from ArrayList itself

        // boolean contains(Object o)
        // Object.get(int index)
        // int indexOf(Object o)
        // int lastIndexOf(Object o)
    }
}